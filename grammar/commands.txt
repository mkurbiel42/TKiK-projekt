// to test created grammar:

antlr4 Python.g4 -o generated
cd generated
javac Python*.java
grun Python file [-tree/-gui/-trace] [filename]
(ommitting filename parses stdin, press CTRL+Z on Windows/CTRL+D on Linux to leave)

// to generate grammar into 